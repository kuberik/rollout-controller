apiVersion: kuberik.com/v1alpha1
kind: Rollout
metadata:
  name: example-rollout
spec:
  releasesImagePolicy:
    name: example-image-policy
  # Optional: specify a specific version to deploy
  # wantedVersion: "1.2.3"
  # Optional: limit the number of history entries to keep
  # versionHistoryLimit: 10
  # Optional: specify bake time for health checks
  # bakeTime: "5m"
  # Optional: specify health check selector
  # healthCheckSelector:
  #   matchLabels:
  #     app: my-app
