---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: kubestatuses.kuberik.com
spec:
  group: kuberik.com
  names:
    kind: KubeStatus
    listKind: KubeStatusList
    plural: kubestatuses
    singular: kubestatus
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubeStatus is the Schema for the kubestatuses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeStatusSpec defines the desired state of KubeStatus.
            properties:
              targetRef:
                description: TargetRef references a namespace-local object whose status
                  should be evaluated.
                properties:
                  apiVersion:
                    description: APIVersion of the target object, e.g. "apps/v1" or
                      "kustomize.toolkit.fluxcd.io/v1beta2"
                    minLength: 1
                    type: string
                  kind:
                    description: Kind of the target object, e.g. "Deployment" or "Kustomization"
                    minLength: 1
                    type: string
                  name:
                    description: Name of the target object in the same namespace as
                      this KubeStatus
                    minLength: 1
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              template:
                description: Template customizes the generated HealthCheck metadata
                  (labels/annotations).
                properties:
                  metadata:
                    description: Metadata contains labels/annotations for the HealthCheck.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations to set on the HealthCheck
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels to set on the HealthCheck
                        type: object
                    type: object
                type: object
            required:
            - targetRef
            type: object
          status:
            description: KubeStatusStatus defines the observed state of KubeStatus.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
